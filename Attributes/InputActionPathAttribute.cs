using UnityEngine;

namespace DBH.Input.Attributes {
    /// <summary>
    /// Displays a string as a project-wide input action selector in the Inspector.
    /// The selected value is stored as "ActionMap/Action".
    /// </summary>
    public sealed class InputActionPathAttribute : PropertyAttribute { }
}
